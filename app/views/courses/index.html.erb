<h1>Course listing</h1>

<div class="d-grid gap-3">
<% @courses.each do |course| %>
    <div class="card">
        <div class="card-body">
          <h3 class="card-title"><%= course.short_name %>: <%= course.name %></h3>
          <p class="card-text"><%= course.description %></p>
          <a href="#" class="btn btn-primary">Go somewhere</a>
          <a href="#" class="btn btn-primary">Go somewhere</a>
        </div>
      </div>
  <% end %>
  </div>


